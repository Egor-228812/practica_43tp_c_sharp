<Window x:Class="Petrov_Tema_16.Views.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Чат" Height="450" Width="550" WindowStartupLocation="CenterScreen">
    <DockPanel Margin="10">
        <ListBox ItemsSource="{Binding Messages}" DockPanel.Dock="Top" Height="350"/>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal">
            <TextBox Text="{Binding NewMessage}" Width="350" Margin="5"/>
            <Button Content="Отправить" Command="{Binding SendCommand}" Margin="5" Width="80" IsEnabled="{Binding IsConnected}"/>
        </StackPanel>
    </DockPanel>
</Window>