<Window x:Class="Petrov_Tema_16.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Petrov_Tema_16"
        Title="Вход в систему бронирования" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <StackPanel>
            <Label Content="Имя пользователя"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="Пароль"/>
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged"/>
            <Button Content="Войти" Command="{Binding LoginCommand}" Margin="0,10,0,5"/>
            <Button Content="Зарегистрировать менеджера" Command="{Binding RegisterCommand}"/>
            <ProgressBar IsIndeterminate="{Binding IsLoading}" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" Height="20" Margin="0,10"/>
        </StackPanel>
    </Grid>
</Window>